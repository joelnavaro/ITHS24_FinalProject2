export enum ButtonType {
  primary,
  secondary,
  success,
  disable,
  textButton,
  complement,
}

export interface UserAppCredentials {
  firstName: string
  lastName: string
  profilePicture: string
}
